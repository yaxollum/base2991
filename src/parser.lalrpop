use super::ast::Ast;
use super::num::Num;
use lalrpop_util::ParseError;

grammar;

pub Ast: Ast = {
    () => Ast::NOP,
    Num
}

Num: Ast = r"\d+[bdox]" =>? match Num::from_str(<>) {Ok(num)=>
    Ok(Ast::Literal(num)),Err(_)=>Err(ParseError::User{error:"invalid literal"}) };
